server:
  port: 8761

spring:
  security:
    user:
      name: heaven
      password: 9lbr62wlprepohoqiruXedruDri2lnuVAz9
  application:
    name: '@name@'
  cloud:
    compatibility-verifier:
      enabled: false

eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
  server:
    wait-time-in-ms-when-sync-empty: 0

management:
  zipkin:
    tracing:
      endpoint: http://localhost:9411/api/v2/spans
  tracing:
    sampling:
      probability: 1.0
